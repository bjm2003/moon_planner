# 总体路线复盘与后续规划

## 当前方向检查

项目当前仍沿着 README 的主线推进：核心库保持 C++17 和 ROS 解耦，主规划链路仍是运动基元、代价地图和状态栅格搜索。近期新增的场景读取、多边形足迹碰撞、安全距离代价、坡度代价和局部目标裁剪都属于阶段 1 到阶段 5 的增量扩展，没有偏离状态栅格/月面局部规划目标。

当前实现已经具备可编译、可测试、可通过 CLI 回放场景的最小闭环，但还不是完整工程产品。后续应优先补齐输入配置、诊断指标、历史/恢复逻辑和批量验证，而不是过早进入 ROS 或 Atlas 适配。

## 已完成能力

- 工程骨架：CMake、核心库、CLI、基础工具和测试目标。
- 基础数据结构：状态、轨迹、栅格索引、占据栅格、高程栅格、代价地图和连续权重历史层。
- 车辆与运动：滑移转向积分、运动约束、运动基元生成和基元库查询。
- 风险建模：多边形车体足迹碰撞、占据障碍、安全距离膨胀、坡度代价和坡度超限 lethal cost。
- 搜索规划：最小 Hybrid A*/状态栅格搜索、路径回溯、轨迹生成。
- 应用入口：`moon_planner_cli` 支持默认场景、轻量 YAML 场景、历史代价场景和默认配置文件。
- 局部规划：`LocalPlanner` 支持按 `planning_horizon_m` 裁剪远距离目标。
- 场景与测试：平坦、密集障碍、坡度、历史障碍场景，以及对应单元/集成测试。

## 主要遗漏

- 配置文件已驱动 CLI、基元生成和 benchmark 的默认参数，但 YAML loader 仍只支持顶层键值、bool、数字和数字数组。
- 场景 YAML 解析仍依赖正则，只适合当前场景子集；复杂场景需要更稳健的解析策略。
- `HistoryLayer` 已支持访问、历史障碍、失败区域权重、衰减和场景输入，但尚未由运行时上下文自动维护。
- `RecoveryPlanner` 已支持 emergency stop 和短距离后退恢复轨迹，但还没有近障卡滞检测、侧向重试或局部目标切换。
- 诊断指标不足，缺少最小障碍距离、最大坡度、代价分量、失败原因分类和场景统计。
- 搜索启发式仍是欧氏距离，复杂障碍和陡坡绕行容易受规划时间上限影响。
- 轨迹平滑、速度连续性和控制参考输出仍是初版。

## 后续开发阶段

### 阶段 A：配置闭环

- 已完成 `YamlLoader` 的基础键值读取能力。
- 已将 `config/planner_default.yaml`、`vehicle_default.yaml`、`cost_weights_default.yaml`、`primitive_default.yaml` 接入 CLI 和工具。
- 已增加配置加载测试，确保默认 C++ 参数和 YAML 参数可对齐。

### 阶段 B：历史层和恢复规划

- 已扩展 `HistoryLayer`，支持历史障碍、已访问区域、失败区域和时间衰减。
- 已将历史层接入 `MapFusion`，形成可测试的历史代价。
- 已将历史层接入场景输入和 `LatticePlanner`/`LocalPlanner` 调用链路。
- 已扩展 `RecoveryPlanner`，支持 emergency stop 和短距离后退恢复轨迹。
- 已增加近障恢复集成测试。
- 后续继续增加侧向候选目标、恢复候选碰撞检查和自动恢复调度。

### 阶段 C：诊断与 Benchmark

- 扩展 `PlannerDiagnostics`，增加最小障碍距离、最大坡度、代价分量、失败分类。
- 扩展 `benchmark_planner`，支持批量场景、重复次数、CSV/文本汇总。
- 固化 1Hz、成功率、安全距离等指标的验收输出。

### 阶段 D：搜索质量和轨迹质量

- 改进启发式，加入航向误差、倒车惩罚或离线启发式表。
- 完善轨迹平滑和速度连续性，输出更接近控制接口的参考速度/角速度。
- 针对密集障碍和坡度绕行场景进行参数回归。

### 阶段 E：系统接口与部署准备

- 梳理 ROS 解耦接口和后续适配层边界。
- 做内存池、预分配和运行时统计，控制动态分配和延迟抖动。
- 保留 Atlas/NPU 适配入口，但不把硬件依赖引入核心库。
